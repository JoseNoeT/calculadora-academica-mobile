export const MIN_WEIGHT = 0;
export const MAX_WEIGHT = 100;
export const TOTAL_WEIGHT = 100;

export const academicStatusRules = {
  minWeight: MIN_WEIGHT,
  maxWeight: MAX_WEIGHT,
  totalWeight: TOTAL_WEIGHT,
} as const;
